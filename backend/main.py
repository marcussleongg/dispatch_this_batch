"""Phase 4: command-center backend — FastAPI REST + WebSocket hub that streams
IncidentState updates (agent tiles, transcripts, findings) to the dashboard.
TODO(Phase 4). Run: uv run --extra backend uvicorn backend.main:app --reload
"""
